# API root, including /api/v1. Use your computer's LAN IP on a physical phone.
# Login and authenticated /auth/me endpoints still need backend implementation.
EXPO_PUBLIC_API_URL=http://localhost:3000/api/v1
